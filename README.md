# A python3 CL tool for converting multiple netxml files into one klm file 

usage: 
```$ ./parse.py -d ~/Desktop/zipdump/netxml -o out`

convert all files in ~/Desktop/zipdump/netxml/*.netxml into ./out.kml
